describe('Team Controller', () => {
    it('should create', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should update', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should delete', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should get team', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });
});
