describe('Vote Controller', () => {
    it('should vote', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should delete', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should get vote', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });
});
