describe('Request Controller', () => {
    it('should ask', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should delete', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });

    it('should get request', () => {
        const result = 5 + 2;
        expect(result).toBe(7);
    });
});
